# ADR-004: Default-deny operational controls

**Status:** Proposed — security approval required.

Enforce strict URL validation, input-size limits, rate limiting, secret externalization, structured security logging, least-privilege database access, and dependency scanning. Event-level analytics requires privacy/retention approval.
