# ADR-003: Relational persistence

**Status:** Proposed — schema approval required.

Use PostgreSQL with migration-managed schema, unique short-code constraints, URL lifecycle fields, and aggregate analytics. This supports integrity, auditability, and straightforward rollback via forward corrective migration.
